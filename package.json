{
  "name": "fungevis-app",
  "version": "1.0.0",
  "description": "FUNGEVIS - Fundación Gerontológica Vida y Salud - Sistema de Gestión",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "db:setup": "cd server && node db/seed.js",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
